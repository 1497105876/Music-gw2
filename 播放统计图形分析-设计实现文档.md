# 播放统计图形分析 — 设计实现文档

> 基于《播放统计图形分析-设计方案.md》，针对 MusicPlayer2-gw 现有代码的实际实现状态，给出完整的改造设计。

---

## 一、现状分析

### 现有代码结构

| 文件 | 作用 |
|------|------|
| `PlayStatistics.h/cpp` | PlayRecord 数据结构 + CPlayStatistics 单例（采集/写入 JSONL） |
| `CPlayStatisticsDlg.h/cpp` | 统计对话框（3个Tab：总览/详细记录/图形分析） |
| `MusicPlayer2.rc` | 对话框资源模板（500×320） |

### 现有图形分析（Tab 2）的问题

1. **只有3个视图**（趋势图/时段图/饼图），设计方案要求5个视图（概览/排行榜/流派分布/30天趋势/时段分布）
2. **没有时间范围筛选**下拉框
3. **`DrawAnalysisChart()` 实际是概览页**，但没有视图切换逻辑——`m_chart_type` 虽然存在但 `% 3` 只在3个图之间循环，而 `DrawAnalysisChart()` 完全不使用 `m_chart_type`
4. **`DrawTrendChart`/`DrawHourChart`/`DrawPieChart`** 三个函数已实现但从未被调用
5. **没有 `GetFilteredRecords()`**，所有统计直接用 `m_records` 全量数据
6. **没有 `StatsAggregate`** 聚合结构，每次绘制都重新遍历计算
7. **`song_length_sec` 实际存的是毫秒**，现有代码未处理这个换算

### 现有控件ID

| ID | 值 | 用途 |
|----|-----|------|
| `IDC_STAT_TAB` | 1400 | Tab 控件 |
| `IDC_STAT_OVERVIEW_LIST` | 1402 | 总览列表 |
| `IDC_STAT_DETAIL_LIST` | 1401 | 详细记录列表 |
| `IDC_STAT_CHART_STATIC` | 1405 | 图表区域 |
| `IDC_EXPORT_CSV_BTN` | 1403 | 导出CSV按钮 |
| `IDC_EXPORT_JSON_BTN` | 1404 | 导出JSON按钮 |

### 需要新增的控件ID

| ID | 建议值 | 用途 |
|----|--------|------|
| `IDC_STAT_TIME_FILTER` | 1406 | 时间范围下拉框 |

---

## 二、改造方案

### 2.1 资源文件修改（MusicPlayer2.rc）

在 `IDD_PLAY_STATISTICS_DIALOG` 对话框模板中，**Tab 控件内右上角**添加时间筛选下拉框：

```rc
IDD_PLAY_STATISTICS_DIALOG DIALOGEX 0, 0, 500, 320
STYLE DS_SETFONT | DS_MODALFRAME | WS_MAXIMIZEBOX | WS_POPUP | WS_CAPTION | WS_SYSMENU
CAPTION "Play Statistics"
FONT 9, "微软雅黑", 400, 0, 0x0
BEGIN
    CONTROL         "",IDC_STAT_TAB,"SysTabControl32",0x0,7,7,486,290
    CONTROL         "",IDC_STAT_OVERVIEW_LIST,"SysListView32",LVS_REPORT | LVS_ALIGNLEFT | WS_BORDER | WS_TABSTOP,12,32,476,260
    CONTROL         "",IDC_STAT_DETAIL_LIST,"SysListView32",LVS_REPORT | LVS_ALIGNLEFT | WS_BORDER | WS_TABSTOP,12,32,476,260
    CONTROL         "",IDC_STAT_CHART_STATIC,"Static",SS_BLACKFRAME | SS_NOTIFY,12,32,476,260
    COMBOBOX        IDC_STAT_TIME_FILTER,380,7,110,120,CBS_DROPDOWNLIST | WS_VSCROLL | WS_TABSTOP
    PUSHBUTTON      "Export CSV",IDC_EXPORT_CSV_BTN,7,300,60,14
    PUSHBUTTON      "Export JSON",IDC_EXPORT_JSON_BTN,75,300,60,14
    DEFPUSHBUTTON   "Close",IDCANCEL,440,300,50,14
END
```

**位置说明**：下拉框 `(380, 7, 110, 120)` 放在 Tab 控件内部右上角，与设计方案一致。

### 2.2 resource.h 新增

```cpp
#define IDC_STAT_TIME_FILTER          1406
```

### 2.3 CPlayStatisticsDlg.h 改造

```cpp
#pragma once
#include "BaseDialog.h"
#include "ListCtrlEx.h"
#include "CTabCtrlEx.h"
#include "PlayStatistics.h"
#include <map>
#include <vector>

class CPlayStatisticsDlg : public CBaseDialog
{
    DECLARE_DYNAMIC(CPlayStatisticsDlg)

public:
    CPlayStatisticsDlg(CWnd* pParent = nullptr);
    virtual ~CPlayStatisticsDlg();

#ifdef AFX_DESIGN_TIME
    enum { IDD = IDD_PLAY_STATISTICS_DIALOG };
#endif

protected:
    // ── 控件 ──
    CTabCtrlEx m_tab;
    CListCtrlEx m_detail_list;       // 详细记录列表
    CListCtrlEx m_overview_list;     // 总览列表
    CStatic m_chart_static;          // 图表显示区域
    CComboBox m_time_filter;         // 时间范围下拉框（新增）

    // ── 数据 ──
    std::vector<PlayRecord> m_records;
    int m_chart_type{ 0 };           // 0=概览, 1=排行榜, 2=流派, 3=趋势, 4=时段
    int m_time_filter_days{ 0 };     // 0=全部, 7/30/90=最近N天（新增）

    // ── 聚合统计结构体（新增）──
    struct StatsAggregate {
        int total_plays{ 0 };
        int total_duration_sec{ 0 };
        int completed{ 0 }, skipped{ 0 }, stopped{ 0 }, errored{ 0 };
        int hour_count[24]{};
        std::map<std::wstring, int> artist_time;   // 艺术家 → 播放时长(秒)
        std::map<std::wstring, int> album_time;    // 专辑 → 播放时长(秒)
        std::map<std::wstring, int> song_count;    // file_path → 播放次数
        std::map<std::wstring, int> genre_time;    // 流派 → 播放时长(秒)
        std::map<std::wstring, int> daily_count;   // YYYY-MM-DD → 播放次数
    };

    // ── 数据过滤与聚合（新增）──
    std::vector<PlayRecord> GetFilteredRecords() const;
    StatsAggregate AggregateStats(const std::vector<PlayRecord>& records) const;

    // ── 绘图函数 ──
    void DrawAnalysisChart();
    void DrawOverviewPage(CDC* pDC, const CRect& rect, const StatsAggregate& stats);
    void DrawRankingPage(CDC* pDC, const CRect& rect, const StatsAggregate& stats);
    void DrawGenrePage(CDC* pDC, const CRect& rect, const StatsAggregate& stats);
    void DrawTrendChart(CDC* pDC, const CRect& rect, const StatsAggregate& stats);
    void DrawHourChart(CDC* pDC, const CRect& rect, const StatsAggregate& stats);

    // ── 辅助绘图函数（新增）──
    void DrawSeparator(CDC* pDC, int x1, int x2, int y);
    void DrawHourBars(CDC* pDC, const CRect& rect, const int hour_count[24], bool show_labels);
    void DrawResultBar(CDC* pDC, const CRect& rect, const StatsAggregate& stats);
    void DrawTopBars(CDC* pDC, const CRect& rect,
                     const std::vector<std::pair<std::wstring, int>>& sorted,
                     int top_n, COLORREF colors[], int name_w);

    // 详细记录列表列索引
    enum DetailColumn
    {
        DCOL_INDEX = 0, DCOL_TIME, DCOL_TITLE, DCOL_ARTIST,
        DCOL_ALBUM, DCOL_PLAY_DUR, DCOL_SONG_LEN, DCOL_RESULT, DCOL_SOURCE,
    };

    enum OverviewColumn
    {
        OCOL_ITEM = 0, OCOL_VALUE,
    };

protected:
    virtual CString GetDialogName() const override;
    virtual bool InitializeControls() override;
    virtual void DoDataExchange(CDataExchange* pDX);

    void LoadRecords();
    void ShowOverview();
    void ShowDetail();
    void ExportData(bool csv_format);
    void InitTimeFilter();  // 新增

    DECLARE_MESSAGE_MAP()
public:
    virtual BOOL OnInitDialog();
    afx_msg void OnTcnSelChangeTab(NMHDR* pNMHDR, LRESULT* pResult);
    afx_msg void OnBnClickedExportCsvButton();
    afx_msg void OnBnClickedExportJsonButton();
    afx_msg void OnPaint();
    afx_msg BOOL OnEraseBkgnd(CDC* pDC);
    afx_msg void OnLButtonDown(UINT nFlags, CPoint point);
    afx_msg void OnCbnSelChangeTimeFilter();  // 新增
};
```

### 2.4 核心新增函数实现

#### 2.4.1 GetFilteredRecords() — 时间过滤

```cpp
std::vector<PlayRecord> CPlayStatisticsDlg::GetFilteredRecords() const
{
    if (m_time_filter_days == 0)
        return m_records;

    time_t now = time(nullptr);
    time_t cutoff = now - (time_t)m_time_filter_days * 86400;

    std::vector<PlayRecord> result;
    for (const auto& r : m_records)
    {
        if (r.played_at.size() >= 19)
        {
            struct tm tm_buf = {};
            tm_buf.tm_year = _wtoi(r.played_at.substr(0, 4).c_str()) - 1900;
            tm_buf.tm_mon  = _wtoi(r.played_at.substr(5, 2).c_str()) - 1;
            tm_buf.tm_mday = _wtoi(r.played_at.substr(8, 2).c_str());
            tm_buf.tm_hour = _wtoi(r.played_at.substr(11, 2).c_str());
            tm_buf.tm_min  = _wtoi(r.played_at.substr(14, 2).c_str());
            tm_buf.tm_sec  = _wtoi(r.played_at.substr(17, 2).c_str());
            time_t t = _mkgmtime(&tm_buf);
            if (t >= cutoff)
                result.push_back(r);
        }
    }
    return result;
}
```

#### 2.4.2 AggregateStats() — 一次性聚合

```cpp
CPlayStatisticsDlg::StatsAggregate CPlayStatisticsDlg::AggregateStats(
    const std::vector<PlayRecord>& records) const
{
    StatsAggregate s;
    for (const auto& r : records)
    {
        s.total_plays++;
        s.total_duration_sec += r.play_duration_sec;

        switch (r.finish_reason)
        {
        case PlayRecord::FinishReason::COMPLETED:  s.completed++; break;
        case PlayRecord::FinishReason::SKIPPED:    s.skipped++;   break;
        case PlayRecord::FinishReason::STOPPED:    s.stopped++;   break;
        case PlayRecord::FinishReason::PLAY_ERROR:  s.errored++;  break;
        }

        // 时段
        if (r.played_at.size() >= 13)
        {
            int hr = _wtoi(r.played_at.substr(11, 2).c_str());
            if (hr >= 0 && hr < 24)
                s.hour_count[hr]++;
        }

        // 日期
        if (r.played_at.size() >= 10)
        {
            std::wstring date = r.played_at.substr(0, 10);
            s.daily_count[date]++;
        }

        // 艺术家/专辑/流派 → 播放时长
        if (!r.artist.empty())
            s.artist_time[r.artist] += r.play_duration_sec;
        if (!r.album.empty())
            s.album_time[r.album] += r.play_duration_sec;
        if (!r.genre.empty())
            s.genre_time[r.genre] += r.play_duration_sec;

        // 歌曲 → 播放次数
        s.song_count[r.file_path]++;
    }
    return s;
}
```

#### 2.4.3 InitTimeFilter() — 初始化下拉框

```cpp
void CPlayStatisticsDlg::InitTimeFilter()
{
    m_time_filter.AddString(L"全部时间");
    m_time_filter.AddString(L"最近7天");
    m_time_filter.AddString(L"最近30天");
    m_time_filter.AddString(L"最近90天");
    m_time_filter.SetCurSel(0);  // 默认全部时间
}
```

#### 2.4.4 OnCbnSelChangeTimeFilter() — 下拉框切换

```cpp
void CPlayStatisticsDlg::OnCbnSelChangeTimeFilter()
{
    int sel = m_time_filter.GetCurSel();
    switch (sel)
    {
    case 0: m_time_filter_days = 0;  break;  // 全部
    case 1: m_time_filter_days = 7;  break;  // 7天
    case 2: m_time_filter_days = 30; break;  // 30天
    case 3: m_time_filter_days = 90; break;  // 90天
    }
    Invalidate();  // 触发重绘
}
```

### 2.5 DrawAnalysisChart() 改造

```cpp
void CPlayStatisticsDlg::DrawAnalysisChart()
{
    // 禁止控件自绘背景
    ::SetWindowLongPtr(m_chart_static.GetSafeHwnd(), GWL_STYLE,
        (::GetWindowLongPtr(m_chart_static.GetSafeHwnd(), GWL_STYLE) & ~SS_BLACKFRAME) | SS_OWNERDRAW);
    m_chart_static.UpdateWindow();

    CRect rect;
    m_chart_static.GetWindowRect(&rect);
    ScreenToClient(&rect);
    if (rect.Width() < 80 || rect.Height() < 80) return;

    // 获取过滤后的数据并聚合
    auto filtered = GetFilteredRecords();
    auto stats = AggregateStats(filtered);

    CDC* pDC = GetDC();
    if (!pDC) return;

    // 背景
    pDC->FillSolidRect(rect, RGB(248, 248, 252));

    // 创建字体
    CFont fTitle, fSection, fText, fHint;
    fTitle.CreatePointFont(160, L"Microsoft YaHei", pDC);
    fSection.CreatePointFont(105, L"Microsoft YaHei", pDC);
    fText.CreatePointFont(85, L"Microsoft YaHei", pDC);
    fHint.CreatePointFont(85, L"Microsoft YaHei", pDC);

    pDC->SetBkMode(TRANSPARENT);

    // 根据当前视图类型绘制
    switch (m_chart_type)
    {
    case 0: DrawOverviewPage(pDC, rect, stats); break;
    case 1: DrawRankingPage(pDC, rect, stats);  break;
    case 2: DrawGenrePage(pDC, rect, stats);    break;
    case 3: DrawTrendChart(pDC, rect, stats);   break;
    case 4: DrawHourChart(pDC, rect, stats);    break;
    }

    ReleaseDC(pDC);
}
```

### 2.6 五个视图绘制函数

#### 视图0：DrawOverviewPage — 概览页

```cpp
void CPlayStatisticsDlg::DrawOverviewPage(CDC* pDC, const CRect& rect,
    const StatsAggregate& stats)
{
    CFont fTitle, fSection, fText, fHint;
    CDC* dc = pDC;
    fTitle.CreatePointFont(160, L"Microsoft YaHei", dc);
    fSection.CreatePointFont(105, L"Microsoft YaHei", dc);
    fText.CreatePointFont(85, L"Microsoft YaHei", dc);
    fHint.CreatePointFont(85, L"Microsoft YaHei", dc);

    dc->SetBkMode(TRANSPARENT);

    int L = rect.left + 24, R = rect.right - 24;
    int y = rect.top + 4;
    wchar_t buf[256];

    // ── 标题 + 视图提示 ──
    dc->SelectObject(&fTitle);
    dc->SetTextColor(RGB(30, 30, 30));
    dc->TextOutW(L, y, L"概览", 2);

    dc->SelectObject(&fHint);
    dc->SetTextColor(RGB(140, 140, 150));
    // 右侧提示
    const wchar_t* hints[] = { L"概览", L"排行榜", L"流派分布", L"30天趋势", L"时段分布" };
    dc->DrawTextW(hints[0], -1, CRect(R - 100, y, R, y + 20),
        DT_RIGHT | DT_SINGLELINE);
    dc->TextOutW(R - 10, y, L"（点击切换 →）", 7);

    y += 24;

    // ── 基本统计文字 ──
    int dur = stats.total_duration_sec;
    int completion_rate = stats.total_plays > 0
        ? stats.completed * 100 / stats.total_plays : 0;
    swprintf_s(buf,
        L"总播放: %d次  │  时长: %d时%02d分%02d秒  │  完整率: %d%%",
        stats.total_plays, dur / 3600, (dur % 3600) / 60, dur % 60,
        completion_rate);
    dc->SelectObject(&fText);
    dc->SetTextColor(RGB(50, 50, 50));
    dc->TextOutW(L, y, buf, (int)wcslen(buf));
    y += 24;

    // 分隔线
    DrawSeparator(dc, L, R, y);
    y += 8;

    // ── 听歌时段柱状图（高度80px）──
    dc->SelectObject(&fSection);
    dc->SetTextColor(RGB(40, 40, 40));
    dc->TextOutW(L, y, L"听歌时段", 4);
    y += 16;

    {
        CRect hour_rect(L, y, R, y + 64);
        DrawHourBars(dc, hour_rect, stats.hour_count, true);
        y = hour_rect.bottom + 4;
    }

    // 分隔线
    DrawSeparator(dc, L, R, y);
    y += 8;

    // ── 播放结果占比条 + 图例 ──
    dc->SelectObject(&fSection);
    dc->SetTextColor(RGB(40, 40, 40));
    dc->TextOutW(L, y, L"播放结果", 4);
    y += 16;

    {
        CRect result_rect(L, y, R, y + 20);
        DrawResultBar(dc, result_rect, stats);
        y = result_rect.bottom + 4;

        // 图例
        dc->SelectObject(&fText);
        dc->SetTextColor(RGB(60, 60, 60));
        int lx = L;
        struct { const wchar_t* lbl; int cnt; COLORREF clr; } pie[] = {
            { L"完整", stats.completed, RGB(90, 175, 90) },
            { L"跳过", stats.skipped,   RGB(225, 145, 70) },
            { L"停止", stats.stopped,   RGB(110, 150, 210) },
            { L"出错", stats.errored,   RGB(215, 90, 90) },
        };
        for (auto& it : pie)
        {
            CBrush br(it.clr);
            dc->FillRect(CRect(lx, y, lx + 12, y + 12), &br);
            int pct = stats.total_plays > 0
                ? (int)((double)it.cnt / stats.total_plays * 100) : 0;
            swprintf_s(buf, L"%s %d(%d%%)", it.lbl, it.cnt, pct);
            dc->TextOutW(lx + 16, y - 1, buf, (int)wcslen(buf));
            lx += 110;
        }
        y += 20;
    }

    // 分隔线
    DrawSeparator(dc, L, R, y);
    y += 8;

    // ── 30天趋势缩略图（约55px高）──
    dc->SelectObject(&fSection);
    dc->SetTextColor(RGB(40, 40, 40));
    dc->TextOutW(L, y, L"30天播放趋势", 7);
    y += 16;

    CRect trend_rect(L, y, R, rect.bottom - 5);
    DrawTrendChart(dc, trend_rect, stats);
}
```

#### 视图1：DrawRankingPage — 排行榜页

```cpp
void CPlayStatisticsDlg::DrawRankingPage(CDC* pDC, const CRect& rect,
    const StatsAggregate& stats)
{
    CDC* dc = pDC;
    CFont fTitle, fSection, fText, fHint;
    fTitle.CreatePointFont(160, L"Microsoft YaHei", dc);
    fSection.CreatePointFont(105, L"Microsoft YaHei", dc);
    fText.CreatePointFont(85, L"Microsoft YaHei", dc);
    fHint.CreatePointFont(85, L"Microsoft YaHei", dc);

    dc->SetBkMode(TRANSPARENT);

    int L = rect.left + 24, R = rect.right - 24;
    int y = rect.top + 4;
    wchar_t buf[256];

    // 标题
    dc->SelectObject(&fTitle);
    dc->SetTextColor(RGB(30, 30, 30));
    dc->TextOutW(L, y, L"排行榜", 3);
    dc->SelectObject(&fHint);
    dc->SetTextColor(RGB(140, 140, 150));
    dc->DrawTextW(L"排行榜（点击切换 →）", -1,
        CRect(R - 160, y, R, y + 20), DT_RIGHT | DT_SINGLELINE);
    y += 24;

    // 三个 section 各占约 78px
    int section_h = (rect.bottom - y - 10) / 3;

    // ── Top 5 艺术家 ──
    dc->SelectObject(&fSection);
    dc->SetTextColor(RGB(40, 40, 40));
    dc->TextOutW(L, y, L"── Top 5 艺术家 ──", 10);
    y += 16;

    {
        std::vector<std::pair<std::wstring, int>> sorted(
            stats.artist_time.begin(), stats.artist_time.end());
        std::sort(sorted.begin(), sorted.end(),
            [](auto& a, auto& b) { return a.second > b.second; });

        COLORREF colors[] = {
            RGB(100,170,230), RGB(100,200,200), RGB(220,120,160),
            RGB(180,220,100), RGB(130,190,130)
        };
        int nameW = 140;
        CRect bar_rect(L, y, R, y + section_h - 20);
        DrawTopBars(dc, bar_rect, sorted, 5, colors, nameW);
        // 显示时长
        dc->SelectObject(&fText);
        dc->SetTextColor(RGB(110, 110, 110));
        for (int i = 0; i < min(5, (int)sorted.size()); i++)
        {
            int dur = sorted[i].second;
            swprintf_s(buf, L"%d:%02d", dur / 60, dur % 60);
            int row_y = bar_rect.top + i * ((bar_rect.Height()) / 5);
            dc->TextOutW(R - 50, row_y + 2, buf, (int)wcslen(buf));
        }
    }
    y = rect.top + 4 + 24 + section_h;

    // ── Top 5 专辑 ──
    dc->SelectObject(&fSection);
    dc->SetTextColor(RGB(40, 40, 40));
    dc->TextOutW(L, y, L"── Top 5 专辑 ──", 9);
    y += 16;

    {
        std::vector<std::pair<std::wstring, int>> sorted(
            stats.album_time.begin(), stats.album_time.end());
        std::sort(sorted.begin(), sorted.end(),
            [](auto& a, auto& b) { return a.second > b.second; });

        COLORREF colors[] = {
            RGB(230,130,100), RGB(240,200,100), RGB(230,170,130),
            RGB(200,160,220), RGB(170,150,230)
        };
        int nameW = 140;
        CRect bar_rect(L, y, R, y + section_h - 20);
        DrawTopBars(dc, bar_rect, sorted, 5, colors, nameW);
        dc->SelectObject(&fText);
        dc->SetTextColor(RGB(110, 110, 110));
        for (int i = 0; i < min(5, (int)sorted.size()); i++)
        {
            int dur = sorted[i].second;
            swprintf_s(buf, L"%d:%02d", dur / 60, dur % 60);
            int row_y = bar_rect.top + i * ((bar_rect.Height()) / 5);
            dc->TextOutW(R - 50, row_y + 2, buf, (int)wcslen(buf));
        }
    }
    y = rect.top + 4 + 24 + section_h * 2;

    // ── Top 5 歌曲 ──
    dc->SelectObject(&fSection);
    dc->SetTextColor(RGB(40, 40, 40));
    dc->TextOutW(L, y, L"── Top 5 歌曲 ──", 9);
    y += 16;

    {
        // song_count 是 file_path → 次数，需要转换为显示名
        std::vector<std::pair<std::wstring, int>> sorted(
            stats.song_count.begin(), stats.song_count.end());
        std::sort(sorted.begin(), sorted.end(),
            [](auto& a, auto& b) { return a.second > b.second; });

        // 提取文件名
        std::vector<std::pair<std::wstring, int>> named;
        for (const auto& [path, cnt] : sorted)
        {
            std::wstring name = path;
            size_t pos = name.find_last_of(L"\\/");
            if (pos != std::wstring::npos) name = name.substr(pos + 1);
            // 去掉扩展名
            size_t dot = name.find_last_of(L'.');
            if (dot != std::wstring::npos) name = name.substr(0, dot);
            named.emplace_back(name, cnt);
        }

        COLORREF colors[] = {
            RGB(130,190,130), RGB(200,160,220), RGB(170,150,230),
            RGB(230,170,130), RGB(240,200,100)
        };
        int nameW = 140;
        CRect bar_rect(L, y, R, y + section_h - 20);
        DrawTopBars(dc, bar_rect, named, 5, colors, nameW);
        // 显示次数
        dc->SelectObject(&fText);
        dc->SetTextColor(RGB(110, 110, 110));
        for (int i = 0; i < min(5, (int)named.size()); i++)
        {
            swprintf_s(buf, L"%d次", named[i].second);
            int row_y = bar_rect.top + i * ((bar_rect.Height()) / 5);
            dc->TextOutW(R - 50, row_y + 2, buf, (int)wcslen(buf));
        }
    }
}
```

#### 视图2：DrawGenrePage — 流派分布环形图

```cpp
void CPlayStatisticsDlg::DrawGenrePage(CDC* pDC, const CRect& rect,
    const StatsAggregate& stats)
{
    CDC* dc = pDC;
    CFont fTitle, fSection, fText, fHint;
    fTitle.CreatePointFont(160, L"Microsoft YaHei", dc);
    fSection.CreatePointFont(105, L"Microsoft YaHei", dc);
    fText.CreatePointFont(85, L"Microsoft YaHei", dc);
    fHint.CreatePointFont(85, L"Microsoft YaHei", dc);

    dc->SetBkMode(TRANSPARENT);

    int L = rect.left + 24, R = rect.right - 24;
    int y = rect.top + 4;

    // 标题
    dc->SelectObject(&fTitle);
    dc->SetTextColor(RGB(30, 30, 30));
    dc->TextOutW(L, y, L"流派分布", 4);
    dc->SelectObject(&fHint);
    dc->SetTextColor(RGB(140, 140, 150));
    dc->DrawTextW(L"流派分布（点击切换 →）", -1,
        CRect(R - 160, y, R, y + 20), DT_RIGHT | DT_SINGLELINE);
    y += 30;

    // 准备数据
    std::vector<std::pair<std::wstring, int>> sorted(
        stats.genre_time.begin(), stats.genre_time.end());
    std::sort(sorted.begin(), sorted.end(),
        [](auto& a, auto& b) { return a.second > b.second; });

    // 如果流派超过5种，第6种开始合并为"Other"
    std::vector<std::pair<std::wstring, int>> display;
    int other_time = 0;
    for (int i = 0; i < (int)sorted.size(); i++)
    {
        if (i < 5)
            display.push_back(sorted[i]);
        else
            other_time += sorted[i].second;
    }
    if (other_time > 0)
        display.emplace_back(L"Other", other_time);

    if (display.empty())
    {
        dc->SelectObject(&fText);
        dc->SetTextColor(RGB(120, 120, 120));
        dc->TextOutW(L, y + 40, L"暂无流派数据（FLAC 文件通常不含流派标签）", 0);
        return;
    }

    int total = 0;
    for (const auto& [name, t] : display) total += t;
    if (total == 0) return;

    // 配色
    COLORREF colors[] = {
        RGB(100,170,230), RGB(230,130,100), RGB(130,190,130),
        RGB(200,160,220), RGB(240,200,100), RGB(180,180,180)
    };

    // 环形图尺寸
    int avail_w = R - L;
    int avail_h = rect.bottom - y - 10;
    int pie_size = min(avail_w - 220, avail_h - 20);  // 右侧留220px给图例
    if (pie_size < 100) pie_size = 100;
    int pie_x = L + 20;
    int pie_y = y + (avail_h - pie_size) / 2;
    int pie_r = pie_size / 2;
    CPoint center(pie_x + pie_r, pie_y + pie_r);

    // 绘制环形图（外圆减内圆）
    int inner_r = pie_r * 2 / 3;
    double start_angle = -90.0;

    for (int i = 0; i < (int)display.size(); i++)
    {
        double sweep = (double)display[i].second / total * 360.0;
        double end_angle = start_angle + sweep;

        double rad_start = start_angle * 3.14159265 / 180.0;
        double rad_end = end_angle * 3.14159265 / 180.0;

        CPoint start_pt(
            center.x + (LONG)(pie_r * cos(rad_start)),
            center.y + (LONG)(pie_r * sin(rad_start)));
        CPoint end_pt(
            center.x + (LONG)(pie_r * cos(rad_end)),
            center.y + (LONG)(pie_r * sin(rad_end)));

        CBrush brush(colors[i % 6]);
        dc->SelectObject(&brush);
        CPen white_pen(PS_SOLID, 1, RGB(255, 255, 255));
        dc->SelectObject(&white_pen);

        // 外圆 Pie
        dc->Pie(
            center.x - pie_r, center.y - pie_r,
            center.x + pie_r, center.y + pie_r,
            start_pt.x, start_pt.y,
            end_pt.x, end_pt.y);

        // 用背景色画内圆遮盖中心
        CBrush bg_brush(RGB(248, 248, 252));
        dc->SelectObject(&bg_brush);
        dc->SelectStockObject(NULL_PEN);
        dc->Ellipse(
            center.x - inner_r, center.y - inner_r,
            center.x + inner_r, center.y + inner_r);

        start_angle = end_angle;
    }

    // 中心文字：流派数量
    dc->SelectObject(&fSection);
    dc->SetTextColor(RGB(50, 50, 50));
    swprintf_s(buf, L"%d种", (int)display.size());
    {
        wchar_t buf2[16];
        swprintf_s(buf2, L"%d种", (int)display.size());
        CRect center_rect(center.x - inner_r, center.y - 10,
                          center.x + inner_r, center.y + 10);
        dc->DrawTextW(buf2, -1, &center_rect,
            DT_CENTER | DT_SINGLELINE | DT_VCENTER);
    }

    // 右侧图例
    dc->SelectObject(&fText);
    int legend_x = pie_x + pie_size + 20;
    int legend_y = y + 10;
    int line_h = 22;

    for (int i = 0; i < (int)display.size(); i++)
    {
        CBrush brush(colors[i % 6]);
        dc->SelectObject(&brush);
        dc->FillRect(CRect(legend_x, legend_y + i * line_h,
            legend_x + 14, legend_y + i * line_h + 14), &brush);

        int pct = (int)((double)display[i].second / total * 100);
        wchar_t buf3[128];
        swprintf_s(buf3, L"%s (%d%%)", display[i].first.c_str(), pct);
        dc->SetTextColor(RGB(60, 60, 60));
        dc->TextOutW(legend_x + 18, legend_y + i * line_h - 1,
            buf3, (int)wcslen(buf3));
    }
}
```

#### 视图3：DrawTrendChart — 30天趋势大图（改造为接受聚合数据）

```cpp
void CPlayStatisticsDlg::DrawTrendChart(CDC* pDC, const CRect& rect,
    const StatsAggregate& stats)
{
    // 生成最近30天日期列表
    time_t now = time(nullptr);
    std::vector<std::wstring> dates;
    std::vector<int> counts;

    for (int i = 29; i >= 0; i--)
    {
        time_t t = now - i * 86400;
        struct tm tm_buf;
        localtime_s(&tm_buf, &t);
        wchar_t buf[16];
        swprintf_s(buf, L"%04d-%02d-%02d",
            tm_buf.tm_year + 1900, tm_buf.tm_mon + 1, tm_buf.tm_mday);
        std::wstring date(buf);
        dates.push_back(date);

        auto it = stats.daily_count.find(date);
        counts.push_back(it != stats.daily_count.end() ? it->second : 0);
    }

    int max_count = 1;
    for (int c : counts) if (c > max_count) max_count = c;

    int margin_left = 50, margin_right = 20, margin_top = 30, margin_bottom = 40;
    int chart_w = rect.Width() - margin_left - margin_right;
    int chart_h = rect.Height() - margin_top - margin_bottom;
    if (chart_w <= 0 || chart_h <= 0) return;

    // 标题
    pDC->SetTextColor(RGB(60, 60, 60));
    pDC->TextOutW(rect.left + margin_left, rect.top + 5,
        L"30天趋势（点击切换 →）", 0);

    // 坐标轴
    CPen axis_pen(PS_SOLID, 1, RGB(180, 180, 180));
    CPen* old_pen = pDC->SelectObject(&axis_pen);
    pDC->MoveTo(rect.left + margin_left, rect.top + margin_top);
    pDC->LineTo(rect.left + margin_left, rect.top + margin_top + chart_h);
    pDC->LineTo(rect.left + margin_left + chart_w, rect.top + margin_top + chart_h);
    pDC->SelectObject(old_pen);

    CFont small_font;
    small_font.CreatePointFont(80, L"Microsoft YaHei", pDC);
    CFont* pOldFont = pDC->SelectObject(&small_font);

    // Y 轴刻度
    pDC->SetTextColor(RGB(120, 120, 120));
    wchar_t buf[16];
    swprintf_s(buf, L"%d", max_count);
    pDC->TextOutW(rect.left + 10, rect.top + margin_top - 5, buf);
    pDC->TextOutW(rect.left + 15, rect.top + margin_top + chart_h - 5, L"0");

    // 柱+折线
    int bar_w = chart_w / 30;
    if (bar_w < 2) bar_w = 2;
    CPen line_pen(PS_SOLID, 2, RGB(80, 140, 220));
    CBrush bar_brush(RGB(100, 170, 230));

    int prev_x = -1, prev_y = -1;
    for (int i = 0; i < 30; i++)
    {
        int x = rect.left + margin_left + chart_w * i / 30 + bar_w / 2;
        int bar_h = (int)((double)counts[i] / max_count * chart_h);
        int y_pos = rect.top + margin_top + chart_h - bar_h;

        pDC->SelectObject(&bar_brush);
        pDC->Rectangle(x - bar_w / 2, y_pos,
            x + bar_w / 2, rect.top + margin_top + chart_h);

        if (prev_x >= 0)
        {
            pDC->SelectObject(&line_pen);
            pDC->MoveTo(prev_x, prev_y);
            pDC->LineTo(x, y_pos);
        }
        prev_x = x;
        prev_y = y_pos;
    }

    // X 轴标签（每5天一个）
    pDC->SetTextColor(RGB(100, 100, 100));
    for (int i = 0; i < 30; i += 5)
    {
        int x = rect.left + margin_left + chart_w * i / 30;
        std::wstring label = dates[i].substr(5);  // MM-DD
        pDC->TextOutW(x, rect.top + margin_top + chart_h + 5,
            label.c_str(), (int)label.size());
    }

    pDC->SelectObject(pOldFont);
    pDC->SelectObject(old_pen);
}
```

#### 视图4：DrawHourChart — 时段分布大图（改造为接受聚合数据）

```cpp
void CPlayStatisticsDlg::DrawHourChart(CDC* pDC, const CRect& rect,
    const StatsAggregate& stats)
{
    int max_count = 1;
    for (int i = 0; i < 24; i++)
        if (stats.hour_count[i] > max_count) max_count = stats.hour_count[i];

    int margin_left = 50, margin_right = 20, margin_top = 30, margin_bottom = 40;
    int chart_w = rect.Width() - margin_left - margin_right;
    int chart_h = rect.Height() - margin_top - margin_bottom;
    if (chart_w <= 0 || chart_h <= 0) return;

    // 标题
    pDC->SetTextColor(RGB(60, 60, 60));
    pDC->TextOutW(rect.left + margin_left, rect.top + 5,
        L"时段分布（点击切换 →）", 0);

    // 坐标轴
    CPen axis_pen(PS_SOLID, 1, RGB(180, 180, 180));
    CPen* old_pen = pDC->SelectObject(&axis_pen);
    pDC->MoveTo(rect.left + margin_left, rect.top + margin_top);
    pDC->LineTo(rect.left + margin_left, rect.top + margin_top + chart_h);
    pDC->LineTo(rect.left + margin_left + chart_w, rect.top + margin_top + chart_h);
    pDC->SelectObject(old_pen);

    CFont small_font;
    small_font.CreatePointFont(80, L"Microsoft YaHei", pDC);
    CFont* pOldFont = pDC->SelectObject(&small_font);

    // Y 轴刻度
    pDC->SetTextColor(RGB(120, 120, 120));
    wchar_t buf[16];
    swprintf_s(buf, L"%d", max_count);
    pDC->TextOutW(rect.left + 10, rect.top + margin_top - 5, buf);
    pDC->TextOutW(rect.left + 15, rect.top + margin_top + chart_h - 5, L"0");

    // 24根柱子
    int bar_w = chart_w / 24;
    if (bar_w < 4) bar_w = 4;

    for (int i = 0; i < 24; i++)
    {
        int x = rect.left + margin_left + chart_w * i / 24;
        int bar_h = (int)((double)stats.hour_count[i] / max_count * chart_h);
        int y_pos = rect.top + margin_top + chart_h - bar_h;

        COLORREF bar_color;
        if (i < 6)       bar_color = RGB(70, 100, 180);    // 凌晨
        else if (i < 12) bar_color = RGB(100, 170, 230);   // 上午
        else if (i < 18) bar_color = RGB(130, 190, 180);   // 下午
        else             bar_color = RGB(160, 130, 200);   // 晚上

        CBrush color_brush(bar_color);
        pDC->SelectObject(&color_brush);
        pDC->Rectangle(x, y_pos, x + bar_w - 1,
            rect.top + margin_top + chart_h);

        if (i % 3 == 0)
        {
            pDC->SetTextColor(RGB(100, 100, 100));
            swprintf_s(buf, L"%d", i);
            pDC->TextOutW(x, rect.top + margin_top + chart_h + 5,
                buf, (int)wcslen(buf));
        }
    }

    // 时段标签
    const wchar_t* period_labels[] = { L"凌晨", L"清晨", L"上午", L"下午", L"傍晚", L"夜晚" };
    int label_positions[] = { 0, 4, 8, 12, 16, 20 };
    pDC->SetTextColor(RGB(110, 110, 110));
    for (int i = 0; i < 6; i++)
    {
        int x = rect.left + margin_left + chart_w * label_positions[i] / 24;
        pDC->TextOutW(x, rect.top + margin_top + chart_h + 20,
            period_labels[i], (int)wcslen(period_labels[i]));
    }

    pDC->SelectObject(pOldFont);
    pDC->SelectObject(old_pen);
}
```

### 2.7 辅助绘图函数

```cpp
void CPlayStatisticsDlg::DrawSeparator(CDC* pDC, int x1, int x2, int y)
{
    CPen pen(PS_SOLID, 1, RGB(210, 210, 215));
    CPen* old = pDC->SelectObject(&pen);
    pDC->MoveTo(x1, y);
    pDC->LineTo(x2, y);
    pDC->SelectObject(old);
}

void CPlayStatisticsDlg::DrawHourBars(CDC* pDC, const CRect& rect,
    const int hour_count[24], bool show_labels)
{
    int max_h = 1;
    for (int i = 0; i < 24; i++)
        if (hour_count[i] > max_h) max_h = hour_count[i];

    int cL = rect.left + 28, cR = rect.right;
    int cT = rect.top, cB = rect.bottom;
    int bw = (cR - cL) / 24;
    if (bw < 3) bw = 3;

    // 基线
    CPen penAxis(PS_SOLID, 1, RGB(180, 180, 180));
    pDC->SelectObject(&penAxis);
    pDC->MoveTo(cL, cB); pDC->LineTo(cR, cB);

    CFont small_font;
    small_font.CreatePointFont(80, L"Microsoft YaHei", pDC);
    pDC->SelectObject(&small_font);

    for (int i = 0; i < 24; i++)
    {
        int bh = hour_count[i] > 0
            ? max(2, (int)((double)hour_count[i] / max_h * (cB - cT - 2)))
            : 0;
        int bx = cL + i * bw;
        COLORREF clr = i < 6 ? RGB(70, 100, 180)
                      : i < 12 ? RGB(100, 170, 230)
                      : i < 18 ? RGB(130, 190, 180)
                      : RGB(160, 130, 200);
        if (bh > 0)
        {
            CBrush br(clr);
            pDC->FillRect(CRect(bx + 1, cB - bh,
                bx + max(1, bw - 2), cB), &br);
        }
        if (show_labels && i % 4 == 0)
        {
            pDC->SetTextColor(RGB(100, 100, 100));
            wchar_t buf[8];
            swprintf_s(buf, L"%02d", i);
            pDC->TextOutW(bx, cB + 3, buf, 2);
        }
    }
    pDC->SelectStockObject(BLACK_PEN);
}

void CPlayStatisticsDlg::DrawResultBar(CDC* pDC, const CRect& rect,
    const StatsAggregate& stats)
{
    int total = stats.total_plays;
    if (total == 0) return;

    struct { int cnt; COLORREF clr; } segments[] = {
        { stats.completed, RGB(90, 175, 90) },
        { stats.skipped,   RGB(225, 145, 70) },
        { stats.stopped,   RGB(110, 150, 210) },
        { stats.errored,   RGB(215, 90, 90) },
    };

    int barL = rect.left;
    int barH = rect.Height();
    for (auto& seg : segments)
    {
        if (seg.cnt == 0) continue;
        int segW = max(seg.cnt > 0 ? 2 : 0,
            (int)((double)seg.cnt / total * rect.Width()));
        CBrush br(seg.clr);
        pDC->FillRect(CRect(barL, rect.top, barL + segW,
            rect.top + barH), &br);
        barL += segW;
    }
}

void CPlayStatisticsDlg::DrawTopBars(CDC* pDC, const CRect& rect,
    const std::vector<std::pair<std::wstring, int>>& sorted,
    int top_n, COLORREF colors[], int name_w)
{
    CFont fText;
    fText.CreatePointFont(85, L"Microsoft YaHei", pDC);
    pDC->SelectObject(&fText);
    pDC->SetBkMode(TRANSPARENT);

    int n = min(top_n, (int)sorted.size());
    if (n == 0) return;

    int max_val = 1;
    for (int i = 0; i < n; i++)
        if (sorted[i].second > max_val) max_val = sorted[i].second;

    int barMaxW = rect.Width() - name_w - 60;
    if (barMaxW < 20) barMaxW = 20;
    int rowH = rect.Height() / top_n;
    if (rowH < 12) rowH = 12;

    for (int i = 0; i < n; i++)
    {
        int rowY = rect.top + i * rowH;

        // 排名+名称
        pDC->SetTextColor(RGB(50, 50, 50));
        std::wstring lbl = std::to_wstring(i + 1) + L". " + sorted[i].first;
        CRect rcName(rect.left, rowY, rect.left + name_w - 8, rowY + rowH);
        pDC->DrawTextW(lbl.c_str(), (int)lbl.size(), &rcName,
            DT_LEFT | DT_SINGLELINE | DT_END_ELLIPSIS | DT_VCENTER);

        // 条形
        int bw2 = max(3, (int)((double)sorted[i].second / max_val * barMaxW));
        CBrush br(colors[i % top_n]);
        pDC->FillRect(CRect(rect.left + name_w, rowY + 3,
            rect.left + name_w + bw2, rowY + rowH - 3), &br);
    }
}
```

### 2.8 OnLButtonDown 改造

```cpp
void CPlayStatisticsDlg::OnLButtonDown(UINT nFlags, CPoint point)
{
    CRect rect;
    m_chart_static.GetClientRect(&rect);
    m_chart_static.MapWindowPoints(this, &rect);

    if (rect.PtInRect(point))
    {
        m_chart_type = (m_chart_type + 1) % 5;  // 改为 %5
        Invalidate();
    }

    CBaseDialog::OnLButtonDown(nFlags, point);
}
```

### 2.9 DoDataExchange 改造

```cpp
void CPlayStatisticsDlg::DoDataExchange(CDataExchange* pDX)
{
    CBaseDialog::DoDataExchange(pDX);
    DDX_Control(pDX, IDC_STAT_TAB, m_tab);
    DDX_Control(pDX, IDC_STAT_DETAIL_LIST, m_detail_list);
    DDX_Control(pDX, IDC_STAT_OVERVIEW_LIST, m_overview_list);
    DDX_Control(pDX, IDC_STAT_CHART_STATIC, m_chart_static);
    DDX_Control(pDX, IDC_STAT_TIME_FILTER, m_time_filter);  // 新增
}
```

### 2.10 OnInitDialog 改造

在 `OnInitDialog()` 中添加时间筛选下拉框初始化：

```cpp
BOOL CPlayStatisticsDlg::OnInitDialog()
{
    CBaseDialog::OnInitDialog();

    // ... 原有 Tab 初始化代码 ...

    // 初始化时间筛选（新增）
    InitTimeFilter();

    // ... 原有 LoadRecords / ShowOverview 代码 ...

    return TRUE;
}
```

### 2.11 MESSAGE_MAP 改造

```cpp
BEGIN_MESSAGE_MAP(CPlayStatisticsDlg, CBaseDialog)
    ON_NOTIFY(TCN_SELCHANGE, IDC_STAT_TAB, &CPlayStatisticsDlg::OnTcnSelChangeTab)
    ON_BN_CLICKED(IDC_EXPORT_CSV_BTN, &CPlayStatisticsDlg::OnBnClickedExportCsvButton)
    ON_BN_CLICKED(IDC_EXPORT_JSON_BTN, &CPlayStatisticsDlg::OnBnClickedExportJsonButton)
    ON_CBN_SELCHANGE(IDC_STAT_TIME_FILTER, &CPlayStatisticsDlg::OnCbnSelChangeTimeFilter)  // 新增
    ON_WM_PAINT()
    ON_WM_ERASEBKGND()
    ON_WM_LBUTTONDOWN()
END_MESSAGE_MAP()
```

---

## 三、改动清单

### 3.1 需修改的文件

| 文件 | 改动内容 |
|------|----------|
| `resource.h` | 新增 `#define IDC_STAT_TIME_FILTER 1406` |
| `MusicPlayer2.rc` | 对话框模板新增 `COMBOBOX IDC_STAT_TIME_FILTER` |
| `CPlayStatisticsDlg.h` | 新增成员变量、函数声明、StatsAggregate 结构体 |
| `CPlayStatisticsDlg.cpp` | 改造 `DrawAnalysisChart()`，新增5个视图绘制函数+辅助函数+过滤聚合函数，改造 `OnLButtonDown` `%5`，改造 `DrawTrendChart`/`DrawHourChart` 签名（接收 StatsAggregate），删除 `DrawPieChart`（被 `DrawGenrePage` 替代），新增消息处理 |

### 3.2 新增函数列表

| 函数 | 所在文件 | 说明 |
|------|----------|------|
| `GetFilteredRecords()` | .cpp | 按时间筛选记录 |
| `AggregateStats()` | .cpp | 一次性聚合统计 |
| `InitTimeFilter()` | .cpp | 初始化下拉框 |
| `OnCbnSelChangeTimeFilter()` | .cpp | 下拉框切换响应 |
| `DrawOverviewPage()` | .cpp | 视图0：概览页 |
| `DrawRankingPage()` | .cpp | 视图1：排行榜页 |
| `DrawGenrePage()` | .cpp | 视图2：流派分布页 |
| `DrawSeparator()` | .cpp | 辅助：画分隔线 |
| `DrawHourBars()` | .cpp | 辅助：画24小时柱状图 |
| `DrawResultBar()` | .cpp | 辅助：画播放结果占比条 |
| `DrawTopBars()` | .cpp | 辅助：画排行榜横向条形图 |

### 3.3 修改函数列表

| 函数 | 改动说明 |
|------|----------|
| `DrawAnalysisChart()` | 从直接画概览改为 switch(5个视图)，使用 GetFilteredRecords + AggregateStats |
| `DrawTrendChart()` | 签名从 `(CDC*, const CRect&)` 改为 `(CDC*, const CRect&, const StatsAggregate&)`，数据从 m_records 改为从 stats.daily_count 读取 |
| `DrawHourChart()` | 同上，从 stats.hour_count 读取 |
| `OnLButtonDown()` | `% 3` 改为 `% 5` |
| `DoDataExchange()` | 新增 `DDX_Control(IDC_STAT_TIME_FILTER, m_time_filter)` |
| `OnInitDialog()` | 新增 `InitTimeFilter()` 调用 |
| `BEGIN_MESSAGE_MAP` | 新增 `ON_CBN_SELCHANGE(IDC_STAT_TIME_FILTER, ...)` |

### 3.4 删除函数

| 函数 | 原因 |
|------|------|
| `DrawPieChart()` | 被 `DrawGenrePage()` 替代（饼图→环形图+图例） |

---

## 四、视图切换流程

```
用户点击图表区域
    │
    ├─ m_chart_type = (m_chart_type + 1) % 5
    │
    ├─ Invalidate()
    │
    └─ OnPaint()
         │
         └─ DrawAnalysisChart()
              │
              ├─ GetFilteredRecords()    ← 受时间筛选影响
              ├─ AggregateStats()        ← 一次性聚合
              │
              └─ switch(m_chart_type)
                   ├─ 0: DrawOverviewPage()   ← 概览（统计+时段+结果+趋势缩略图）
                   ├─ 1: DrawRankingPage()    ← 排行榜（Top5×3）
                   ├─ 2: DrawGenrePage()      ← 流派环形图
                   ├─ 3: DrawTrendChart()     ← 30天趋势大图
                   └─ 4: DrawHourChart()      ← 时段分布大图
```

---

## 五、数据流

```
JSONL 文件 (statistics/playlog_YYYY-MM.jsonl)
    │
    ├─ LoadRecords() → m_records (全量，按时间倒序)
    │
    ├─ GetFilteredRecords() → filtered (按 m_time_filter_days 过滤)
    │
    └─ AggregateStats(filtered) → StatsAggregate {
            total_plays, total_duration_sec,
            completed/skipped/stopped/errored,
            hour_count[24],
            artist_time, album_time, song_count, genre_time,
            daily_count
        }
```

---

## 六、注意事项

### 6.1 song_length_sec 毫秒问题
设计方案指出 `song_length_sec` 实际存的是毫秒。现有代码在 `ShowDetail()` 中直接显示 `song_length_sec`，需要 `÷1000` 换算：
```cpp
m_detail_list.SetItemText(row, DCOL_SONG_LEN, format_dur(r.song_length_sec / 1000).c_str());
```

### 6.2 genre 数据可能为空
FLAC 文件通常不含流派标签，`DrawGenrePage()` 需要处理空数据情况（已设计"暂无流派数据"提示）。

### 6.3 0秒记录
快速切歌时 `play_duration_sec = 0`，这些记录仍计入播放次数但不应计入播放时长排行。当前设计不做过滤，保持与总览数据一致。

### 6.4 时间筛选影响范围
时间筛选影响 Tab 2（图形分析）的所有5个视图。Tab 0（总览）和 Tab 1（详细记录）暂不受时间筛选影响，保持原有全量数据展示。如果需要让总览和详细记录也受筛选影响，可在 `ShowOverview()` 和 `ShowDetail()` 中调用 `GetFilteredRecords()`。

### 6.5 DrawTrendChart/DrawHourChart 的双用途
在概览页（视图0）中，`DrawTrendChart` 被作为缩略图调用；在视图3中，它被作为大图调用。两者使用相同的函数，只是 `rect` 参数不同。`DrawHourBars` 是概览页用的简化版时段柱状图，`DrawHourChart` 是视图4用的完整版。

### 6.6 字体创建优化
当前设计在每个绘制函数中创建字体。如果性能有问题，可以将字体改为成员变量，在 `OnInitDialog` 中创建一次。对于 500×320 的对话框，每次重绘创建4个字体的开销可以忽略。

### 6.7 对话框尺寸
对话框 500×320，Tab 控件 486×290，图表区域 476×260。所有布局参数基于 476×260 的可用区域计算。如果后续调整对话框尺寸，需要同步更新各视图的布局参数。
