# 播放统计对话框设计文档

## 概述

MusicPlayer2-gw 播放统计对话框是一个基于 Tab 页的多标签统计界面，展示用户的播放历史数据。
采用 MFC 原生框架开发，使用 GDI 自绘图表，不依赖任何第三方图表库。

---

## 窗口架构

### 类继承关系

```
CDialog
  └─ CBaseDialog          // 项目基类：统一背景色、字体、图标、翻译、窗口大小记忆
       └─ CTabDlg          // 标签页子对话框基类：管理滚动、Tab 生命周期
            ├─ CStatOverviewTabDlg     // 概览页
            ├─ CStatArtistRankTabDlg   // 歌手排行页
            ├─ CStatSongRankTabDlg     // 歌曲排行页
            ├─ CStatTrendTabDlg        // 趋势页
            └─ CStatSongsTabDlg        // 歌曲详情页
```

### 主对话框 CPlayStatisticsDlg

- 继承自 `CBaseDialog`（不是 `CDialog`）
- 持有 `CTabCtrlEx m_tab` 作为 Tab 容器
- 持有 5 个子页面对象作为成员变量
- `OnInitDialog` 中创建子页面、传递数据、添加到 Tab
- 不手动处理 `OnSize`，完全依赖动态布局

### 子对话框 CTabDlg

- 继承自 `CBaseDialog`，但 `GetDialogName()` 返回空（不独立记忆窗口大小）
- `IsRememberDialogSizeEnable()` 返回 false（由父窗口统一管理）
- 重写 `ResizeDynamicLayout()` 控制动态布局的调用时机（处理滚动场景）
- 提供 `OnTabEntered()` / `OnTabExited()` 虚函数供标签切换时回调

### Tab 容器 CTabCtrlEx

- 继承自 `CTabCtrl`
- `AddWindow()` 添加子窗口并自动设置父窗口
- `AdjustTabWindowSize()` 计算并调整所有子窗口大小
- **关键：`OnSize()` 自动调用 `AdjustTabWindowSize()`** —— 当 Tab 控件被缩放时，内部子页面自动跟随
- `SetCurTab()` 切换标签页时显示/隐藏对应子窗口

---

## 布局系统（核心）

### 三层缩放链路

```
用户拖动主窗口
  → CMFCDynamicLayout 自动缩放 IDC_STAT_TAB（Tab 控件）
    → CTabCtrlEx::OnSize() 调用 AdjustTabWindowSize()
      → 子页面 MoveWindow() 跟随 Tab 控件缩放
        → CTabDlg::ResizeDynamicLayout() 调用 CMFCDynamicLayout::Adjust()
          → 子页面内部控件按 AFX_DIALOG_LAYOUT 规则缩放
```

### AFX_DIALOG_LAYOUT 格式

每个对话框在 .rc 文件中有一段 `AFX_DIALOG_LAYOUT`：

```
IDD_xxx AFX_DIALOG_LAYOUT
BEGIN
    0,                    // 总开关：0=启用
    x_move%, y_move%, x_size%, y_size%   // 每个控件一行
END
```

**百分比含义**（相对于窗口缩放量）：
- `0, 0, 0, 0` = 完全不动（固定位置和大小）
- `0, 0, 100, 100` = 不移动，宽高都跟随窗口缩放
- `0, 100, 0, 0` = y 跟随底部（按钮常驻底部）
- `100, 100, 0, 0` = 锚定右下角（关闭按钮）
- `38, 0, 62, 100` = x 跟随38%处，宽度跟随62%，高度跟随100%

### 各布局规则

#### 主对话框 IDD_PLAY_STATISTICS_DIALOG (500x320)

| 控件 | 位置 | Layout | 说明 |
|------|------|--------|------|
| IDC_STAT_TAB | 7,7,486,290 | `0,0,100,100` | Tab 容器宽高跟随 |
| IDC_EXPORT_CSV_BTN | 7,300,60,14 | `0,100,0,0` | 左下角，y跟随底部 |
| IDC_EXPORT_JSON_BTN | 75,300,60,14 | `0,100,0,0` | 左下角，y跟随底部 |
| IDCANCEL | 440,300,50,14 | `100,100,0,0` | 右下角锚定 |

#### 歌手排行 IDD_STAT_ARTIST_RANK_DLG (476x260)

| 控件 | 位置 | Layout | 说明 |
|------|------|--------|------|
| IDC_STAT_ARTIST_RANK_CHART | 0,0,180,260 | `0,0,100,100` | 图表宽高跟随 |
| IDC_STAT_ARTIST_RANK_LIST | 180,0,296,260 | `38,0,62,100` | 列表x跟随38%，宽62%，高跟随 |

#### 歌曲排行 IDD_STAT_SONG_RANK_DLG (476x260)

同歌手排行，控件 ID 不同。

#### 概览/歌曲/趋势页

单控件铺满：`0,0,100,100`

### 为什么不能手动 OnSize

之前 `CPlayStatisticsDlg::OnSize()` 手动 `MoveWindow` 移动 Tab 和按钮，导致：
1. 与 `CMFCDynamicLayout` 冲突（两套布局机制打架）
2. 子页面内部控件不缩放（只移动了容器，没有触发子页面内部布局）
3. 按钮位置在恢复窗口大小时不正确

正确做法是**只使用 AFX_DIALOG_LAYOUT**，让 MFC 框架的动态布局管理器全权处理。

---

## 图表绘制

### 自绘模式

所有图表使用 `SS_OWNERDRAW` 静态控件 + `OnDrawItem` 消息实现：

1. 资源中放 `SS_BLACKFRAME` 静态控件
2. `OnInitDialog` 中用 `SetWindowLongPtr` 改样式为 `SS_OWNERDRAW`
3. 响应 `WM_DRAWITEM` 消息，获取 `LPDRAWITEMSTRUCT` 中的 DC 和矩形
4. 用 GDI 绘制图表内容

### 优势

- 不需要额外的图表库依赖
- 与项目现有代码风格一致（趋势页已使用此模式）
- 完全控制绘制逻辑和样式
- 自动响应窗口缩放（因为控件本身被动态布局缩放了）

### 三种图表

#### 水平条形图（歌手排行、歌曲排行）

- 最多显示 Top 15
- 每条高度 = 可用高度 / 条数（自适应）
- 颜色渐变：歌手用蓝→紫→橙，歌曲用绿→青→蓝
- 每条显示：排名 + 名称（截断6字）+ 数值
- 条形宽度 = (值 / 最大值) × (可用宽度 - 50px文字区)

#### 柱状图 + 折线图（趋势页）

- 30天播放次数趋势
- 柱状图 + 折线图叠加
- 包含坐标轴、Y轴刻度、X轴日期标签

---

## 数据流

```
PlayRecord 结构体
  ├── file_path      (文件路径)
  ├── title          (标题)
  ├── artist         (艺术家)
  ├── album          (专辑)
  ├── genre          (流派)
  ├── played_at      (播放时间 ISO 格式)
  ├── play_duration_sec (播放时长秒)
  ├── song_length_sec   (歌曲总长秒)
  ├── finish_reason  (结束原因：播完/跳过/停止/出错)
  └── ...
```

### 数据加载

1. 主对话框 `LoadRecords()` 扫描 `config_dir/statistics/playlog_*.jsonl` 文件
2. 逐行解析 JSONL（手写解析，非 JSON 库）
3. 按时间倒序排序
4. 通过 `SetRecords()` 传递给各子页面

### 各页数据聚合

- **概览页**：今日/本周/本月/累计统计 + 播放结果分布 + 24小时时段分布
- **歌手排行**：按 artist 聚合 play_duration_sec，降序
- **歌曲排行**：按 file_path 计数，降序
- **趋势页**：按日期(played_at 前10字符)聚合计数
- **歌曲页**：原始记录列表

### 数据过滤

所有统计页统一过滤 `play_duration_sec < 15` 的记录（播放不足15秒不计入统计）。

---

## 导出功能

### CSV 导出

- UTF-8 BOM 编码（Excel 兼容）
- 字段：序号、播放时间、标题、艺术家、专辑、播放时长、歌曲长度、结果、来源
- CSV 转义：含逗号或引号的字段用双引号包裹，内部引号双写

### JSON 导出

- UTF-8 编码
- 每条记录调用 `PlayRecord::ToJson()` 序列化
- 格式化缩进输出

---

## 关键设计要点

1. **不要在子页面 OnInitDialog 中初始化列宽** —— 因为此时窗口尺寸可能还是资源中的初始值。列宽应在 `SetRecords()` 填充数据时设置。

2. **SS_OWNERDRAW 控件必须在 OnInitDialog 中改样式** —— 资源里写 `SS_BLACKFRAME` 是为了让对话框编辑器显示边框，运行时改成 `SS_OWNERDRAW` 才能触发自绘。

3. **图表 Invalidate 时用 FALSE 参数** —— `m_chart.Invalidate(FALSE)` 只重绘客户区不擦除背景，避免闪烁。

4. **OnDrawItem 中检查矩形尺寸** —— 太小时直接返回，避免在窗口最小化或刚创建时绘制出错。

5. **GDI 对象选择** —— 绘制完毕后恢复原始 font/pen/brush，防止 GDI 资源泄漏。

6. **RepositionTextBasedControls** —— 在 `InitializeControls()` 中调用，根据本地化文本长度自动调整按钮宽度。返回 true 触发 `ReLoadLayoutResource()` 重新加载动态布局参数。

---

## 文件清单

| 文件 | 职责 |
|------|------|
| CPlayStatisticsDlg.h/.cpp | 主对话框：Tab 管理、数据加载、CSV/JSON 导出 |
| StatOverviewTabDlg.h/.cpp | 概览页：今日/本周/本月统计、播放结果、时段分布 |
| StatArtistRankTabDlg.h/.cpp | 歌手排行：列表 + 水平条形图 |
| StatSongRankTabDlg.h/.cpp | 歌曲排行：列表 + 水平条形图 |
| StatTrendTabDlg.h/.cpp | 趋势页：30天柱状图 + 折线图 |
| StatSongsTabDlg.h/.cpp | 歌曲详情：完整播放记录列表 |
| PlayStatistics.h/.cpp | PlayRecord 结构体定义与序列化 |
| CTabCtrlEx.h/.cpp | Tab 控件扩展：子窗口管理、自动缩放 |
| TabDlg.h/.cpp | 标签页基类：滚动、生命周期 |
| BaseDialog.h/.cpp | 对话框基类：背景、字体、翻译、窗口记忆 |

---

## 版本历史

- **v1.0** (b283ca3): 播放统计功能完整实现（数据采集+界面+图表）
- **v1.1** (c739b9f): 修复 ListCtrl 滚动条崩溃
- **v1.2** (b4aac2b): 修复 Debug 版崩溃 + 菜单文字 error + 类型安全
- **v1.3** (0951247): 完成显示图表分析（5个 Tab 页、GDI 图表）
- **v1.5** (3892ea5): 完整的第一代安装包
- **v2.0**: 排行页添加水平条形图 + 修复窗口缩放布局问题（AFX_DIALOG_LAYOUT）
